---
description: Deploy the project to Vercel
---
1. Run the command `npm i -g vercel` to install the Vercel CLI globally.
// turbo
2. Run the command `vercel login` to log in to Vercel.
3. Run the command `vercel --prod` to deploy the project to Vercel.
